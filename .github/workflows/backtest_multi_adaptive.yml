name: Backtest Multi Adaptive

on:
  workflow_dispatch:

jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.10'
      - run: pip install requests
      - name: Run backtest (multi)
        run: python .github/backtest_multi_adaptive.py
        
