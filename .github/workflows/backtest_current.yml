name: Backtest (current algo)

on:
  workflow_dispatch:

jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.10'
      - run: pip install --quiet requests
      - name: Backtest current algo
        run: python -u .github/analyze_backtest_current.py
